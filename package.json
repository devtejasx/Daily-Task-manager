{
  "name": "hunter-command-center-app",
  "version": "1.0.0",
  "description": "Gamified daily task manager (Solo-Leveling-style) built with React + Vite, backed by Firebase Auth and Firestore",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "preview": "npm run preview --workspace=frontend",
    "test": "npm run test --workspace=frontend",
    "test:watch": "npm run test:watch --workspace=frontend"
  }
}
